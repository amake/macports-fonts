# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0

categories          fonts
name                BabelStoneHanPUA
version             1.306
revision            0
epoch               1
description         A free CJK font containing 5,200+ unencoded Han characters in the Private Use Area
long_description    ${description}
platforms           darwin
supported_archs     noarch
maintainers         {amake @amake} openmaintainer
license             free

homepage            https://babelstone.co.uk/Fonts/PUA.html
master_sites        https://babelstone.co.uk/Fonts/Download/

distfiles           ${name}.ttf
dist_subdir         ${name}/${version}_${revision}_${epoch}

extract.only

use_configure       no

checksums           rmd160  69e0ea16a56a070863cf56638889aa395e34adef \
                    sha256  64b56efcda943f0570434359631f07671b3db035352736bc472ba446f19c53fa \
                    size    4865420

build {}

destroot.violate_mtree yes

destroot {
    set target ${destroot}/Library/Fonts/
    xinstall -m 0755 -d ${target}
    copy ${distpath}/${distfiles} ${target}
}

livecheck.type      regex
livecheck.regex     {>([0-9.]+) \(}
