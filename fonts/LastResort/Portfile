# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0

categories          fonts
github.setup        unicode-org last-resort-font 15.000
name                LastResort
revision            0

description         A special-purpose font meant to serve as a fallback when other \
                    fonts don't have a glyph for a given character
long_description    ${description}

platforms           darwin
supported_archs     noarch
maintainers         {amake @amake} openmaintainer
license             OFL

github.tarball_from releases

use_configure       no

checksums           rmd160  c8e9dd9bd47a42c32adebb558e9d0ff32a76bd51 \
                    sha256  b5a6cbd70646342aca928c619d544e2c844d73ba346db256b421ebaac02c5fd1 \
                    size    8429216

subport ${name}HE {
    long_description \
        ${description}. The high-efficiency version is much smaller but \
        potentially less compatible, for instance with Windows or Adobe apps.

    checksums       rmd160  316c979db1a88fcb9fd16cf9996c91d90b245da6 \
                    sha256  432a3fb6e06f0479c6fcb5bcb14032eb6c697aa9577f27f08d409baf8bc911a8 \
                    size    526928
}

distfiles           ${subport}-Regular.ttf
dist_subdir         ${subport}/${version}

extract.only

build {}

destroot.violate_mtree yes

destroot {
    set target ${destroot}/Library/Fonts/
    xinstall -m 0755 -d ${target}
    copy ${distpath}/${distfiles} ${target}
}
