# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0

categories          fonts
github.setup        unicode-org last-resort-font 14.000
name                LastResort
revision            0

description         A special-purpose font meant to serve as a fallback when other \
                    fonts don't have a glyph for a given character
long_description    ${description}

platforms           darwin
supported_archs     noarch
maintainers         {amake @amake} openmaintainer
license             OFL

github.tarball_from releases

use_configure       no

checksums           rmd160  e51fbb0a5093f6027b5f053c0247e229ebdbac58 \
                    sha256  da83a62294e74d963a10de4c3750ccf089273e3b7fc6744daef9844163ade078 \
                    size    8270360

subport ${name}HE {
    long_description \
        ${description}. The high-efficiency version is much smaller but \
        potentially less compatible, for instance with Windows or Adobe apps.

    checksums       rmd160  45476018acc9e8a0f17c3b02062562457b3a3331 \
                    sha256  74507aa7ae9b02a469a6acbba53db83f978b76a89e8ccec95bb5bc14941588f9 \
                    size    518424
}

distfiles           ${subport}-Regular.ttf
dist_subdir         ${subport}/${version}

extract.only

build {}

destroot.violate_mtree yes

destroot {
    set target ${destroot}/Library/Fonts/
    xinstall -m 0755 -d ${target}
    copy ${distpath}/${distfiles} ${target}
}
