# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0

categories          fonts
github.setup        huertatipografica piazzolla 2.004 v
name                Piazzolla
revision            0
github.tarball_from releases

description         Type system intended for optimizing the available space in \
                    press media and other publications.
long_description    ${description}. It has a compact appearance which allows for \
                    small font sizes and tight leading while achieving solid lines \
                    and robust paragraphs.

homepage            https://piazzolla.huertatipografica.com/

platforms           darwin
maintainers         {amake @amake} openmaintainer
license             OFL

use_configure       no
use_zip             yes

distname            ${name}
dist_subdir         ${name}/${version}

set smallcaps_dist  ${name}SC${extract.suffix}

distfiles           ${distname}${extract.suffix} \
                    ${name}SC${extract.suffix}

checksums           ${distname}${extract.suffix} \
                        rmd160  d17692a133a052a001f43521b18423bb1c74259d \
                        sha256  132e467a79121da12551d18f84209b8584b5dca40f4366d040076086cae4f254 \
                        size    7489355 \
                    ${smallcaps_dist} \
                        rmd160  40bc2bb366f0b09ea22695a060ea3522ec7c2b53 \
                        sha256  657739ecbc9566d03b21432ed1c44a78bf8b872ce35fc9154f410bfbaa005947 \
                        size    5177971

build {}

destroot.violate_mtree yes

destroot {
    set target ${destroot}/Library/Fonts/
    xinstall -m 0755 -d ${target}
    copy {*}[glob ${workpath}/*/static/otf/*.otf] ${target}
}
